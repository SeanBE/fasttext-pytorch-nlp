# fastText vs pyTorch with Quora.

Using the recently released dataset from Quora, demonstrate the abilities of fastText and pyTorch to learn word vectors. 


## Installation

`pip install -r requirements.txt`
